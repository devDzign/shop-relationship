{% extends 'base.html.twig' %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-12 col-lg-6 col-xl-6">
            {{ form_start(form) }}
            <div class="card">
                <div class="card-header">
                    <h1 class="mb-0">Inscription</h1>
                </div>
                <div class="card-body">
                    {{ form_rest(form) }}
                </div>
                <div class="card-footer">


                    <button class="btn btn-block btn-primary"> <i class="fa fa-edit"></i> S'inscrire</button>
                    <a
                            {#                            href="{{ path("security_login") }}" #}
                            class="btn btn-block btn-outline-primary"
                    >
                        <i class="fa fa-sign-in-alt"></i> Se connecter
                    </a>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
